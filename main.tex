% Gonski Lab -- SLAC Open House poster
% Built on the Gemini theme: https://rev.cs.uchicago.edu/k4rtik/gemini-uccs
% A fork of https://github.com/anishathalye/gemini

\documentclass[final]{beamer}

\pdfcompresslevel=0
\pdfobjcompresslevel=0

% ====================
% Packages
% ====================

\usepackage[T1]{fontenc}
\usepackage{lmodern}

\usepackage[scale=1.2, size=a0, orientation=portrait]{beamerposter}
\setlength{\paperheight}{46.8in}
\setlength{\paperwidth}{33.1in}

\usetheme{gemini}
\usecolortheme{stanford}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}

\usepackage{siunitx}
\usepackage{enumitem}
\usepackage{multirow}
\usepackage{colortbl}
\usepackage{xcolor}
\usepackage{anyfontsize}
\usepackage[caption=false]{subfig}
\usepackage{amsmath}
\usepackage{makecell}

\usetikzlibrary{arrows.meta, positioning, shapes.geometric, calc}

% ====================
% Custom colors
% ====================
\definecolor{stageblue}{RGB}{41,98,163}
\definecolor{stagegreen}{RGB}{0,128,85}
\definecolor{stageorange}{RGB}{200,120,20}
\definecolor{stagepurple}{RGB}{100,60,150}
\definecolor{tcr}{RGB}{180,30,30}
\definecolor{tabheader}{RGB}{140,21,21}
\definecolor{tabheadertext}{RGB}{255,255,255}
\definecolor{tabrow1}{RGB}{255,245,245}
\definecolor{tabrow2}{RGB}{255,255,255}

\date{\today}

% Spacing
\setlength{\textfloatsep}{5pt}
\setlength{\abovecaptionskip}{2pt}
\setlength{\belowcaptionskip}{0pt}

% ====================
% Font overrides
% ====================
\setbeamerfont{headline title}{size=\huge,series=\bfseries}
% Drop to \large if the student line ever wraps in the header
\setbeamerfont{headline author}{size=\large}

% % ====================
% Lengths
% ====================

\newlength{\sepwidth}
\newlength{\colwidth}
\setlength{\sepwidth}{0.025\paperwidth}
\setlength{\colwidth}{0.45\paperwidth}

\newcommand{\separatorcolumn}{\begin{column}{\sepwidth}\end{column}}

% ====================
% Helpers
% ====================

% Short lead-in sentence for a block, set slightly larger than body text
\newcommand{\lead}[1]{{\large\textbf{#1}}\par\vspace{0.2cm}}

% Small caption under a figure
\newcommand{\figcap}[1]{\vspace{0.15cm}{\small\textit{#1}}}

% Sub-heading inside a block
\newcommand{\subhead}[1]{%
  \vspace{0.4cm}{\large\bfseries\color{cardinalred}#1}\par\vspace{0.2cm}}

% Reserved space for a section that still needs content
\newcommand{\placeholder}[1]{%
  \vspace{0.3cm}
  \begin{center}
    \colorbox{boxgray}{%
      \parbox{0.92\columnwidth}{%
        \centering\vspace{1.2cm}%
        \textit{\textcolor{coolgray}{#1}}%
        \vspace{1.2cm}}}%
  \end{center}
  \vspace{0.3cm}}

% ====================
% Title
% ====================

\title{Machine Learning \& Microelectronics for\\New Physics Searches at Colliders}

% Two lines: seniors on the first, students on the second.
\author{%
  \textbf{PI:} Julia Gonski \quad \textbf{Postdocs:} Sagar Addepalli, Qibin Liu \\[0.8ex]
  \textbf{PhD:} Kenny Jia, Liangyu Wu \quad \textbf{Undergrads:} Gia Ancone, Deniz Yilmaz, Alexander Yue%
}

% No \institute and no \footercontent: the header carries the title and authors
% only, and the gemini footline template draws nothing when \footercontent is
% undefined, so the poster has no bottom band at all.

% ====================
% Headline layout
% ====================

% Override the gemini headline: title and members flush left, logos flush right,
% both on one line. Widths are tuned so the pair fills the page with even margins.
\newcommand{\headtextwidth}{0.66\paperwidth}
\newcommand{\headlogowidth}{0.20\paperwidth}

\setbeamertemplate{headline}
{
  \begin{beamercolorbox}[wd=\paperwidth]{headline}
    \vskip6ex
    \hbox to \paperwidth{%
      \hskip\sepwidth
      \begin{minipage}[c]{\headtextwidth}
        \raggedright
        \usebeamerfont{headline}
        {\usebeamerfont{headline title}\usebeamercolor[fg]{headline title}\inserttitle\par}
        \vskip3ex
        {\usebeamerfont{headline author}\usebeamercolor[fg]{headline author}\insertauthor\par}
      \end{minipage}%
      \hfill
      \ifdefined\insertlogoright\insertlogoright\fi
      \hskip\sepwidth
    }%
    \vskip6ex
    \ifbeamercolorempty[bg]{headline rule}{}{
      \begin{beamercolorbox}[wd=\paperwidth,colsep=0.5ex]{headline rule}\end{beamercolorbox}
    }
  \end{beamercolorbox}
}

% ====================
% Logos (right side, stacked)
% ====================

\logoright{%
  \begin{minipage}[c]{\headlogowidth}
    \centering
    \includegraphics[width=0.92\linewidth]{stanford_logos/SLAC_primary_red.png}\\[1.0cm]
    \includegraphics[height=6.0cm]{stanford_logos/Block_S_2_color.png}
  \end{minipage}%
}

% ====================
% Body
% ====================

\begin{document}

\begin{frame}[t]
\begin{columns}[t]
\separatorcolumn

\begin{column}{\colwidth}

  % ---------- 1. Trigger anomaly detection ----------
  \begin{block}{Anomaly Detection in the ATLAS Trigger}

    \begin{columns}[T]
    \begin{column}{0.70\columnwidth}
      \lead{Looking for anything that isn't ``normal.''}

      Machine learning-based anomaly detection in offline ATLAS searches can enable
      broad sensitivity to \emph{any} kind of new physics that is not Standard
      Model-like. Pushing this capability down to the \textbf{trigger level} --- the
      ultra-low-latency system that decides which collisions to keep --- gives us the
      potential to write out never-before-seen events from the LHC.
    \end{column}
    \begin{column}{0.26\columnwidth}
      \centering
      \includegraphics[width=0.85\columnwidth]{figures/openhouse/atlas_logo.png}
    \end{column}
    \end{columns}

    \vspace{0.4cm}
    We use a \textbf{VAE-GAN} trained over collision events, modeled by the
    four-vectors of their constituent particles. The network learns to reconstruct
    ordinary events; events it reconstructs \emph{badly} are exactly the ones worth
    saving.

    \vspace{0.4cm}
    \begin{figure}
      \centering
      \includegraphics[width=0.72\columnwidth]{figures/openhouse/vae_gan.png}

      \figcap{The VAE-GAN: an encoder compresses each event, a decoder rebuilds it,
      and a discriminator sharpens the result. Reconstruction error becomes the
      anomaly score.}
    \end{figure}

    \vspace{0.4cm}
    To run these algorithms inside the tight latency and resource budget of the
    ATLAS trigger, we employ \textbf{``fast ML''} techniques: compression
    (truncating low-weight nodes), quantization, and reducing the number of loss
    terms.

    \vspace{0.4cm}
    \begin{figure}
      \centering
      \includegraphics[width=0.80\columnwidth]{figures/openhouse/anomaly_roc.png}

      \figcap{One trigger, many signals: a single anomaly detection model is
      sensitive to a wide range of new physics models it was never trained on.}
    \end{figure}

    \vspace{0.3cm}
    \textbf{Status:} the first ATLAS anomaly trigger is on track for data-taking in
    2025, and studies for Run 4 anomaly triggers are ongoing!

  \end{block}


  % ---------- 2. Large-scale ML ----------
  \begin{block}{Large-Scale ML for Particle Physics}

    \lead{Foundation models and contrastive learning for collider data.}

    \subhead{Foundation models for HEP}
    \placeholder{Content to be added.}

    \subhead{Contrastive learning}
    \placeholder{Content to be added.}

  \end{block}


  % ---------- 3. New physics searches ----------
  \begin{block}{Searching for New Physics}

    \subhead{Dark sector mediators}

    Potential \textbf{dark matter} candidates can be produced in the decays of heavy
    scalar particles. These dark matter particles can be \textbf{long-lived}, and
    decay far away from the primary proton collision point --- leaving behind
    \textbf{displaced decay vertices} rather than tracks that all point back to one
    spot.

    \vspace{0.4cm}
    \begin{figure}
      \centering
      \includegraphics[width=0.62\columnwidth]{figures/openhouse/dark_sector_feynman.png}

      \figcap{A heavy scalar $S$ decays to a pair of dark photons $Z_d$, each of
      which travels before decaying to an electron-positron pair.}
    \end{figure}

    \vspace{0.3cm}
    Using new reconstruction techniques, we can identify these displaced vertices in
    the ATLAS \textbf{inner tracker} and in the \textbf{calorimeter}, decaying to
    pairs of electrons.

    \subhead{Anomaly detection in Higgs final states}

    Anomaly detection is also used to look for signs of new physics decaying into
    \textbf{electron or photon final states with unusual topologies} --- signatures
    that no dedicated search would have been designed to catch.

    \placeholder{Higgs anomaly detection results to be added.}

  \end{block}

\end{column}

\separatorcolumn

\begin{column}{\colwidth}

  % ---------- 4. Edge ML ----------
  \begin{block}{Edge ML \& Smart Detectors}

    \lead{Put the intelligence where the data is born.}

    Machine learning at the detector \textbf{``edge''} --- the source of the data ---
    can reduce the off-detector data rate and enable a far more efficient trigger and
    data acquisition system. Rather than shipping every raw sample off the detector,
    a smart front-end sends only what matters.

    \vspace{0.4cm}
    \begin{figure}
      \centering
      \includegraphics[width=0.72\columnwidth]{figures/openhouse/drift_cell_ml.png}

      \figcap{Feature extraction at the edge: a whole pattern of ionization clusters
      in a drift cell is compressed by a small network into the few numbers that the
      rest of the experiment actually needs.}
    \end{figure}

    \vspace{0.3cm}
    In collaboration with SLAC's \textbf{Technology Innovation Directorate (TID)}, we
    are exploring \textbf{eFPGAs} for ML-based signal processing, leveraging
    innovative data compression algorithms in advanced tracker and calorimeter
    concepts.

    \vspace{0.4cm}
    For example, detectors at next-generation colliders such as CERN's
    \textbf{Future Circular Collider} are looking at \textbf{drift chamber
    trackers}; we develop small compression models that perform key feature
    extraction for deployment directly in the front-end readout.

    \vspace{0.4cm}
    \begin{figure}
      \centering
      \includegraphics[width=0.62\columnwidth]{figures/openhouse/efpga_board.png}

      \figcap{An eFPGA test chip on its carrier board (right: the bare die under a
      microscope) --- a reconfigurable ML engine small enough to live on the
      detector.}
    \end{figure}

    \vspace{0.3cm}
    \textbf{Embedded field programmable gate arrays (eFPGAs)} allow the design of an
    ASIC that is usable at the detector front-end while still carrying a
    \textbf{reconfigurable} digital logic fabric --- so the algorithm can be updated
    long after the chip is built.

  \end{block}


  % ---------- 5. Advancing ML on FPGAs ----------
  \begin{block}{Advancing ML \& Quantum ML on FPGAs}

    \lead{Making state-of-the-art architectures fit in real hardware.}

    Also with TID, we develop novel methods to implement machine learning on
    \textbf{FPGAs}, targeting \textbf{ultra-fast inference} and \textbf{low power}
    for applications like online data acquisition and processing.

    \vspace{0.4cm}
    \begin{figure}
      \centering
      \includegraphics[width=\columnwidth]{figures/openhouse/mlpmixer_to_fpga.png}

      \figcap{From architecture to arithmetic to silicon: the MLP-Mixer is rewritten
      as cheap add/shift operations, then mapped onto FPGA logic.}
    \end{figure}

    \vspace{0.3cm}
    The \textbf{MLP-Mixer} architecture --- promising for resource-limited,
    transformer-like learning --- has been successfully implemented. This enables
    edge and real-time ML to \textbf{scale up} and take advantage of state-of-the-art
    ML techniques, rather than being stuck with only the smallest networks.

    \vspace{0.4cm}
    \begin{figure}
      \centering
      \includegraphics[width=0.92\columnwidth]{figures/openhouse/fpga_rack.png}

      \figcap{Larger models are split across multiple FPGAs in a single server rack,
      linked by high-speed streaming interfaces.}
    \end{figure}

    \subhead{Quantum machine learning}
    \placeholder{Content to be added.}

  \end{block}


  % ---------- Team ----------
  \begin{block}{Who We Are}

    We are an experimental particle physics group at \textbf{SLAC National
    Accelerator Laboratory}, working across the \textbf{Fundamental Physics} and
    \textbf{Technology Innovation} Directorates and with \textbf{Stanford
    University}. Our work spans the full chain: from asking what new physics might
    look like, to training the network, to taping out the chip that runs it.

    \vspace{0.4cm}
    \textbf{Interested?} We host students at every level, and there is room for
    people who love physics, machine learning, hardware, or all three. Come find
    someone with a Gonski Lab badge and say hello.

  \end{block}

\end{column}

\separatorcolumn

\end{columns}
\end{frame}

\end{document}
